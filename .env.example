NEXT_PUBLIC_SITE_URL=https://sorrytomorrow.com/
# Optional. Leave blank to disable the privacy-first analytics beacon.
NEXT_PUBLIC_CF_WEB_ANALYTICS_TOKEN=
# Optional PostHog write-only public project token; never an admin/personal API key.
# The client remains disabled on localhost, previews, and non-production origins.
NEXT_PUBLIC_POSTHOG_KEY=
